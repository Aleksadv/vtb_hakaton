# Шаблон локального файла с секретами для скрипта run_scan.bat.
# Нужно создать файл scanner.env (в корне проекта) и заполните реальные значения локально.

# Переменные:
# CLIENT_ID           — team id (например teamX)
# CLIENT_SECRET       — секрет (client_secret)
# REQUESTING_BANK     — идентификатор банка/команды 
# INTERBANK_CLIENT    — client_id клиента для межбанковых запросов (например teamX-1)

# Поведение скрипта:
# - run_scan.bat пытается прочитать scanner.env.
# - Если scanner.env отсутствует, скрипт попросит ввести CLIENT_ID и CLIENT_SECRET в консоли.